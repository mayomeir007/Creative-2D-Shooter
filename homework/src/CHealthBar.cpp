#include "CHealthBar.hpp"

void CHealthBar::Draw(Vector2 ownerPosition, float healthFraction) const
{
  // TODO: Draw at ownerPosition + m_offset: background rect + fill rect
  // scaled by healthFraction, always axis-aligned.
}
